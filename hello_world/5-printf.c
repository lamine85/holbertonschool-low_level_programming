#include <stdio.h>
#include <stdlib.h>

/**
 * main
 * printf -imprime une phrase
 * Return: (SUCCES)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
